# Blocco accesso HTTP a /config/
# Contiene credenziali DB e costanti applicative.
# MAI esporre via web.

Require all denied

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
